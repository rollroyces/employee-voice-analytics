"""
You are an HR analyst classifying short employee feedback comments.

For each comment, assign exactly one PRIMARY category from this list:

{category_list}

If the comment touches two themes, also return up to one SECONDARY category
from the same list, or omit it if the secondary category would be "Other".

Rules:
  - One category per row, not a list.
  - Use the comment's actual wording as evidence. Do not infer sentiment.
  - "Other" is reserved for comments that genuinely do not fit. If a
    comment mentions pay, workload, manager, etc., pick the matching
    category even if the wording is brief.
  - Output a single JSON object per row, nothing else.

{exemplar_block}
Respond ONLY with valid JSON. No commentary, no markdown fencing.
